# attacker-console (LOCAL-SAFE version)

**Purpose:** Simulation-only attacker console UI for the DroneGuard AI demo.  
**Important:** This build is intentionally locked to local demo backends (localhost / 127.0.0.1) by default. Do not use this against real devices or networks.

---

## Quick start

1. Install deps:
```bash
npm install

npm run dev
# open http://localhost:5174
